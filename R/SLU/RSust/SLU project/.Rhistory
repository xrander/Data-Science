text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 0,
unique(pop$ffert),
col = par(pop$fert)))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 10,
unique(pop$ffert),
col = par(pop$fert)))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 60,
unique(pop$ffert),
col = par(pop$fert)))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 70,
unique(pop$ffert),
col = par(pop$fert)))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 70,
unique(pop$ffert),
col = par(plcol)))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 70,
unique(pop$ffert),
fill = "red"
col = par()))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 70,
unique(pop$ffert),
fill = "red",
col = par()))
fill = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 5, y = 70,
unique(pop$ffert),
fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol))
plcol<- c(20,10)
pchdata <- c(19, 21)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol,
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
legend(x = 6, y = 600,
unique(pop$ffert),
fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
legend(x = 5, y = 500,
unique(pop$ffert),
fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
legend(x = 4, y = 350,
unique(pop$ffert),
fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
legend(x = 4.5, y = 350,
unique(pop$ffert),
fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300, labels = "Relation between height and diameter",col = "red", cex = 0.9),
legend(x = 4.7, y = 350,
unique(pop$ffert),
fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Height vs Diameter",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,
labels = "Diagram of the height and diameter of fertilized and unfertilized seedlings",
col = "red", cex = 0.9),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Diagram of the height and diameter of fertilized and unfertilized seedlings",
col = "red", cex = 0.9),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
plcol<- c(20,10)
pchdata <- c(19, 21)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Diagram of the height and diameter of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#importing the dataframe from user directory and it is assigned to the pop
pop <- read.table("C:/Users/aduol/Documents/Data Science/Data-Science/R/SLU/RSust/SLU project/Data/popdata.txt", header = T, sep = "\t", na.strings = "NA", dec = ".", strip.white = T)
#printing the first six and last five of the dataframe
#for first six values + the header
head(pop)
#printing the last six values + the header
tail(pop)
#printing the names of the variables
names(pop)
#checking the properties of the dataframe
str(pop)
#getting a short overview of the dataframe, giving basic descriptive statistics
# like mean, median, mode, min and max value 25th and 75th quartile
summary(pop)
#reading the first row and column
pop[1,1]
#reading the first four rows and column 3 and 4
pop[1:4,c(3,4)]
#a simpler code with similar result is:
pop[1:4, 3:4]
#another way is by calling the column name
pop[1:4, c('height', 'dia')]
#giving names to the values of fertilized data
pop$ffert <- ifelse(pop$fer==1, 'fert', 'control')
View(pop)
# num of fertilized seedlings
fertilized = sum(pop$fert == 1)
fertilized
# num of unfertilized seedlings
unfertilized = sum(pop$fert != 1)
unfertilized
#getting unique values of blocks and clones
unique(pop$block)
unique(pop$clone)
range(pop$height)
#minimum diameter
min(pop$dia)
#max height
max(pop$height)
#deriving height to diameter ratio
pop$hd <- pop$height/pop$dia
#getting the mean height to diameter ratio
mean(pop$hd)
plcol<- c(20,10)
pchdata <- c(19, 21)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
legend(x = 4, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
legend(x = 4, y = 350, unique(pop$ffert), fill = plcol),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend(x = 4, y = 350, unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.5),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=300,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=0.9, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend(x = 4.7, y = 350, unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend("bottom corner", unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend("bottomright", unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol,
legend("topright", unique(pop$ffert), fill = plcol))
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol)
plcol<- c(20,10)
pchdata <- c(23, 21)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol)
plcol<- c(20,10)
pchdata <- c(15, 16)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol, pch= pchdata)
plcol<- c(20,10)
pchdata <- c(15, 16)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol, pch= pchdata)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), fill = plcol, pch= pchdata)
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), pch= pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) + legend("topright", unique(pop$ffert), pch= pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) +
legend("topright", unique(pop$ffert), pch= pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) +
legend("topright", unique(pop$ffert), pch= pchdata, col = plcol)
pop <- read.table("C:/Users/aduol/Documents/Data Science/Data-Science/R/SLU/RSust/SLU project/Data/popdata.txt", header = T, sep = "\t", na.strings = "NA", dec = ".", strip.white = T)
#for first six values + the header
head(pop)
#printing the last six values + the header
tail(pop)
#printing the names of the variables
names(pop)
#checking the properties of the dataframe
str(pop)
#getting a short overview of the dataframe, giving basic descriptive statistics
# like mean, median, mode, min and max value 25th and 75th quartile
summary(pop)
#reading the first row and column
pop[1,1]
#reading the first four rows and column 3 and 4
pop[1:4,c(3,4)]
#a simpler code with similar result is:
pop[1:4, 3:4]
#another way is by calling the column name
pop[1:4, c('height', 'dia')]
#giving names to the values of fertilized data
pop$ffert <- ifelse(pop$fer==1, 'fert', 'control')
View(pop)
# num of fertilized seedlings
fertilized = sum(pop$fert == 1)
fertilized
# num of unfertilized seedlings
unfertilized = sum(pop$fert != 1)
unfertilized
#getting unique values of blocks and clones
unique(pop$block)
unique(pop$clone)
range(pop$height)
#minimum diameter
min(pop$dia)
#max height
max(pop$height)
#deriving height to diameter ratio
pop$hd <- pop$height/pop$dia
#getting the mean height to diameter ratio
mean(pop$hd)
plcol<- c(20,10)
pchdata <- c(15, 16)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) +
legend("topright", unique(pop$ffert), pch= pchdata, col = plcol)
#plotting a simple graph of height vs diameter
plot(pop$dia, pop$height,
text(x=01.2, y=500,labels = "Growth rate of fertilized and unfertilized seedlings",col = "red", cex = 0.9),
xlim = c(0,5.5), ylim = c(0, 600),xlab = 'diameter(mm)',
ylab = 'Height(mm)', main = "Olamide Adu",
pch = pchdata, col = plcol) +
legend("topright", legend = c("Control","Fertilized"), pch= pchdata, col = plcol)
